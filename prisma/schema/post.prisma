model Post {
    id         String     @id @default(uuid())
    title      String     @db.VarChar(255)
    content    String     @db.Text
    thumbnail  String?
    isFeatured Boolean    @default(false)
    status     PostStatus @default(DRAFT)
    tags       String[]
    views      Int        @default(0)
    authorId   String
    createdAt  DateTime   @default(now())
    updatedAt  DateTime   @updatedAt
    comments   Comment[]

    // indexing
    // if we need to change the table name into database but we use always everywhere model name
    @@index([authorId])
    @@map("posts")
}

// enums
enum PostStatus {
    DRAFT
    PUBLISHED
    ARCHIVED
}
